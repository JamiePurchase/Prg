function styleColour(strColour)
{
	colorRGB(255,255,255)
	if(strColour=="white"){colorRGB(255,255,255)}
	if(strColour=="black"){colorRGB(0,0,0)}
	if(strColour=="slate"){colorRGB(210,230,185)}
	kill(strColour)
}
